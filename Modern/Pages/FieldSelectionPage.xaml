<UserControl x:Class="Microsoft.SqlServer.Utils.Misc.SQLCallStackResolver.Modern.FieldSelectionPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AllowDrop="True" PreviewDragOver="Page_DragOver" Drop="Page_Drop">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock FontSize="16" FontWeight="SemiBold" Margin="0,0,0,4"
                       Text="Import from XEL Files"/>
            <TextBlock FontSize="12" Foreground="Gray"
                       Text="Select XEL file(s), choose the fields to extract, then click Next to import callstacks."/>
        </StackPanel>

        <!-- File picker section -->
        <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Padding="8,4" Click="BrowseXEL_Click"
                        IsEnabled="{Binding IsNotProcessing}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xED25;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Browse XEL files..."/>
                    </StackPanel>
                </Button>
                <TextBlock Grid.Column="1" x:Name="FileInfoText" Margin="12,0,0,0"
                           VerticalAlignment="Center" Foreground="Gray" TextTrimming="CharacterEllipsis"
                           Text="No files selected"/>
            </Grid>
        </Border>

        <!-- Fields header + Group checkbox -->
        <Grid Grid.Row="2" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"
                       Text="Available Fields"/>
            <CheckBox Grid.Column="1" Content="Group events from XEL files"
                      IsChecked="{Binding GroupXEvents}"/>
        </Grid>

        <ListBox Grid.Row="3" x:Name="FieldsListView" SelectionMode="Extended"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsChecked}" Margin="2,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Category}" Foreground="Gray" Width="55" FontSize="12"/>
                            <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </CheckBox>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Add Item" Padding="8,4" Margin="0,0,8,0" Click="AddItem_Click"/>
            <Button Content="Delete Selected" Padding="8,4" Margin="0,0,8,0" Click="DeleteItem_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
