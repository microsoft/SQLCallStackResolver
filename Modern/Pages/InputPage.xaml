<UserControl x:Class="Microsoft.SqlServer.Utils.Misc.SQLCallStackResolver.Modern.InputPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock FontSize="16" FontWeight="SemiBold" Margin="0,0,0,4"
                       Text="Provide Callstack Input"/>
            <TextBlock FontSize="12" Foreground="Gray"
                       Text="Paste or type raw callstack text below, or drag and drop text files."/>
        </StackPanel>

        <!-- Single-line mismatch warning banner (hidden by default) -->
        <Border Grid.Row="1" x:Name="SingleLineWarningBanner" Visibility="Collapsed"
                Background="{DynamicResource WarningBackgroundBrush}" BorderBrush="{DynamicResource WarningBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource WarningForegroundBrush}"/>
                    <TextBlock x:Name="SingleLineWarningText" Foreground="{DynamicResource WarningForegroundBrush}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="AutoFixButton" Padding="10,6" Margin="8,0,0,0"
                        VerticalAlignment="Center" Click="AutoFixSingleLine_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE90F;" FontFamily="Segoe MDL2 Assets" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Fix automatically"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <TextBox Grid.Row="2" x:Name="callStackInput" AcceptsReturn="True" AcceptsTab="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="13" TextWrapping="NoWrap"
                 MaxLength="999999999"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 AllowDrop="True" PreviewDragOver="CallStackInput_DragOver" Drop="CallStackInput_Drop"/>

        <Expander Grid.Row="3" x:Name="AdvancedOptionsExpander" Margin="0,8,0,0" IsExpanded="False">
            <Expander.Header>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                               VerticalAlignment="Center" Margin="0,0,6,0" Foreground="Gray"/>
                    <TextBlock Text="Advanced input options" Foreground="Gray" FontSize="12"/>
                </StackPanel>
            </Expander.Header>
            <WrapPanel Margin="24,6,0,0">
                <CheckBox Content="Callstack frames are on a single line" Margin="0,0,16,4"
                          IsChecked="{Binding FramesOnSingleLine}"/>
                <CheckBox Content="Re-lookup source (rare, needs private PDBs)" Margin="0,0,16,4"
                          IsChecked="{Binding RelookupSource}"/>
            </WrapPanel>
        </Expander>
    </Grid>
</UserControl>
